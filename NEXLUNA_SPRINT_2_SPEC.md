# Nexluna Sprint 2 Build Spec

## 1. الهدف

نبني مصدرًا canonical واحدًا لتعريفات وحدات Nexluna حتى لا تتكرر جداول الوحدات بين محرك التحويل وطبقات الوكلاء والاختبارات. يخدم ذلك المستخدم العربي الذي يحتاج نتائج ثابتة، والمطور الذي يحتاج صيانة آمنة، مع الحفاظ على الحساب الحتمي وعدم إدخال خدمة خارجية.

## 2. نطاق Sprint

| النطاق | القرار |
|---|---|
| داخل النطاق | إنشاء `data/units.json` كمصدر البيانات المعلن؛ توليد ملف JavaScript قابل للتحميل في المتصفح؛ جعل `converter.js` يقرأ البيانات المولدة؛ الحفاظ على الفئات الأربع عشرة والوحدات والعوامل والصيغ والأمثلة الحالية؛ إضافة فحص يثبت تطابق البيانات المولدة مع المصدر؛ توثيق أمر التوليد. |
| خارج النطاق | تغيير واجهة المستخدم؛ إضافة AI؛ تغيير WebMCP contract؛ إضافة وحدات جديدة؛ تغيير عوامل التحويل؛ نقل Smart Search بالكامل إلى JSON؛ نشر أو تعديل حسابات خارجية. |
| القيود | Static-first، بلا backend أو dependency مدفوعة، RTL، العمل دون شبكة بعد تحميل الملفات، وعدم كسر API `window.NexConvert`. |

## 3. المتطلبات القابلة للاختبار

| المعرف | الأولوية | المتطلب | معيار القبول |
|---|---|---|---|
| `REQ-DATA-01` | Must | يوجد مصدر JSON canonical يحتوي الفئات الأربع عشرة وتعريفات وحداتها. | يقرأ validator المصدر ويثبت وجود 14 فئة، وعدم وجود معرفات مكررة داخل الفئة، ووجود الوحدة الأساسية أو نوع التحويل الخاص. |
| `REQ-DATA-02` | Must | يُنتج المصدر ملف JavaScript ثابتًا صالحًا للتحميل قبل المحرك. | ينجح generator، ويحتوي الناتج على كائن البيانات نفسه، ولا يحتاج إلى شبكة أو API. |
| `REQ-DATA-03` | Must | يستخدم `converter.js` البيانات المولدة بدل جدول مستقل مكرر. | لا توجد نسخة ثانية من جدول `DATA` داخل `converter.js`، وتعمل `window.NexConvert` على جميع الفئات. |
| `REQ-DATA-04` | Must | لا تتغير النتائج الحالية. | تنجح بوابة `test_conversions.py` كاملة، وتنجح حالات Smart Search الحالية، مع فحوص خاصة للفدان/القيراط/السهم ودرجة الحرارة والوقود. |
| `REQ-DATA-05` | Should | يمكن إعادة توليد الملف الناتج بأمر واحد واضح. | `python3 scripts/generate_units.py --check` ينجح دون تعديل، و`--write` ينتج ملفًا مطابقًا وقابلًا للمقارنة في Git. |
| `REQ-DATA-06` | Should | يفشل التحقق برسالة واضحة عند حذف فئة أو وحدة أو عامل. | اختبار validator سلبي يثبت رفض schema ناقص، دون تعديل ملفات المشروع. |

## 4. شكل البيانات

كل فئة تحتوي `icon` و`label` و`title` و`units`. الفئة الخطية تحتوي `base` وعوامل التحويل، بينما فئات `temperature` و`fuel` تحتوي `kind` واضحًا بدل افتراض وجود عامل خطي. تحتفظ `formulas` و`common` بالنصوص الحالية لأنها جزء من واجهة الشرح، ولا تُستخدم لتنفيذ الحساب.

## 5. خطة الاختبار

سيُشغّل فحص schema/validator، ثم generator check، ثم اختبارات التحويل الأصلية، ثم regression البحث الطبيعي، ثم فحص صياغة JavaScript وdiff whitespace. لا يعتمد أي اختبار على الإنترنت. يُعد Sprint ناجحًا فقط إذا مرّت كل البوابات ولم تتغير نتائج المحرك.

## 6. المخاطر والافتراضات

الافتراض المحافظ هو إبقاء شكل `DATA` الداخلي كما هو قدر الإمكان، مع نقل مصدره إلى ملف مولد حتى تكون التغييرات قابلة للعكس. الخطر الرئيسي هو اختلاف ترتيب المفاتيح أو فقد precision؛ لذلك سيُحافظ المصدر على الأرقام الحالية حرفيًا ويُضاف اختبار snapshot للعوامل الحساسة. WebMCP سيبقى خارج هذا Sprint لتجنب تغيير عقد تجريبي أثناء نقل البيانات.

## 7. قرار التنفيذ

يبدأ التنفيذ بإضافة المصدر والـgenerator والـvalidator، ثم استبدال الجدول المضمن بمُدخل generated data، ثم تشغيل الاختبارات. لا يُسمح بإضافة تحسينات UI أو AI ضمن هذا Sprint.
