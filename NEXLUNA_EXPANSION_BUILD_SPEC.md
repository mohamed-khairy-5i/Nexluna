# Nexluna Expansion Build Spec

## 1. الملخص والهدف

نبني مرحلة توسع فوق Nexluna v1.0.0 لتحويله من إصدار تقني عربي حتمي إلى منتج أوضح بصريًا، قابل للتوسع اللغوي، ومنظم المحتوى. الهدف هو أن يرى المستخدم فرقًا واضحًا في الواجهة، وأن يستطيع الموقع تقديم نسخة إنجليزية كاملة مع بنية تسمح بإضافة لغات أخرى لاحقًا، وأن تُدار المقالات والبيانات الوصفية من مصادر منظمة بدل HTML عربي موزع داخل مولدات الصفحات.

يظل NexConvert مصدر الحقيقة للحسابات. لا يجوز لنظام الترجمة أو المحتوى أو الشرح أو الواجهة أن يغير عوامل التحويل أو الناتج الرقمي. لا يتضمن هذا النطاق دمج Calcuzakat أو نشرًا خارجيًا أو إنشاء حسابات مستخدمين.

## 2. المستخدمون والرحلات الأساسية

المستخدم الأساسي هو شخص يريد تحويل قيمة بسرعة من الهاتف أو سطح المكتب، بالعربية أو الإنجليزية. الرحلة الأساسية هي فتح الصفحة، اختيار اللغة، كتابة أو اختيار سؤال، رؤية النتيجة، نسخها أو مشاركة الرابط، وفهم طريقة الحساب عند طلب الشرح. الرحلة الثانية هي قراءة مقال تعليمي ثم الانتقال إلى محوّل الفئة أو زوج التحويل المرتبط. الرحلة الثالثة هي تضمين widget في موقع آخر مع الحفاظ على عقد الحساب الحتمي.

## 3. النطاق وخارج النطاق

| التصنيف | النطاق |
|---|---|
| داخل النطاق | إعادة تصميم مرئية للصفحة الرئيسية وصفحات المحوّل وembed مع responsive/mobile-first layout |
| داخل النطاق | إضافة بنية i18n منظمة وإطلاق الإنجليزية كأول لغة كاملة بعد العربية |
| داخل النطاق | الحفاظ على العربية RTL وإضافة LTR للإنجليزية مع عناوين ووصف وJSON-LD مناسبة لكل لغة |
| داخل النطاق | نقل بيانات المقالات والـFAQ والبيانات الوصفية إلى ملفات محتوى منظمة قابلة للتوليد |
| داخل النطاق | ربط المقالات بالفئات والأزواج، وإضافة صفحات تعليمية إنجليزية أساسية |
| داخل النطاق | توثيق WebMCP وواجهة embed بطريقة متعددة اللغة دون تغيير العقد الحسابي |
| خارج النطاق | دمج Calcuzakat |
| خارج النطاق | قاعدة بيانات أو لوحة تحكم CMS أو تسجيل دخول |
| خارج النطاق | نشر خارجي أو شراء نطاق أو تشغيل تحليلات طرف ثالث |
| خارج النطاق | نماذج ذكاء اصطناعي داخل مسار الحساب؛ التفسير يظل اختياريًا وغير سلطوي |
| خارج النطاق | ترجمة جميع اللغات دفعة واحدة؛ النسخة الأولى ستطلق الإنجليزية وتجهز بنية اللغات اللاحقة |

## 4. المتطلبات الوظيفية ومعايير القبول

| المعرف | الأولوية | المتطلب القابل للاختبار | معيار القبول |
|---|---|---|---|
| REQ-UI-01 | Must | عندما يفتح المستخدم الصفحة الرئيسية، يرى تسلسلًا واضحًا للعنوان، البحث، النتيجة، الفئات، الفوائد، والمحتوى | ينجح smoke بصري/DOM على سطح المكتب والهاتف، ولا تختفي عناصر الرحلة الأساسية |
| REQ-UI-02 | Must | عندما يستخدم المستخدم محوّل فئة، تظهر القيمة والوحدات والنتيجة والشرح في بطاقة بصرية متماسكة | تبقى كل selectors والنتيجة وزر الشرح قابلة للاستخدام مع keyboard وRTL/LTR |
| REQ-UI-03 | Must | عندما يضيق العرض إلى شاشة هاتف، لا يحدث overflow أفقي وتظل أهداف اللمس قابلة للاستخدام | فحص viewport بعرض 360px و390px ينجح دون قص أو تداخل حرج |
| REQ-UI-04 | Should | عندما يبدّل المستخدم المظهر أو اللغة، يحافظ الموقع على الاختيار في التنقل داخل نفس اللغة | تظل `lang` و`dir` والعناوين والروابط متسقة بعد إعادة التحميل |
| REQ-I18N-01 | Must | عندما يختار المستخدم English، تُعرض الواجهة الرئيسية وصفحات الفئات وembed بنص إنجليزي كامل | لا توجد نصوص عربية واجهة غير مترجمة في المسارات الإنجليزية الأساسية |
| REQ-I18N-02 | Must | عندما يزور المستخدم النسخة العربية، تبقى المسارات والواجهة الحالية متوافقة | تنجح اختبارات regression الحالية وتبقى العربية RTL |
| REQ-I18N-03 | Must | عندما تتولد صفحة لغة، يكون لها `lang`, `dir`, canonical وalternate hreflang صحيح | كل صفحة أساسية تحتوي على metadata locale قابلة للتحقق ولا تشير إلى URL غير موجود |
| REQ-I18N-04 | Should | عندما يختار المستخدم لغة لاحقة مستقبلًا، يمكن إضافة ملف ترجمة دون نسخ منطق التحويل | توجد translation contract وfallback موثقة ويمنع الاختبار المفاتيح الناقصة |
| REQ-CONT-01 | Must | عندما يولد الموقع مقالًا أو FAQ، تأتي العناوين والوصف والمحتوى من dataset منظم | لا يعتمد مولد المقالات على HTML عربي موزع خارج dataset المحتوى |
| REQ-CONT-02 | Must | عندما يقرأ المستخدم مقالًا، يجد رابطًا إلى محوّل مرتبط وفئة مرتبطة وأسئلة شائعة | ينجح اختبار الروابط الداخلية وعدم وجود روابط مكسورة |
| REQ-CONT-03 | Should | عندما توجد نسخة إنجليزية لمقال، تُربط بالنسخة العربية عبر alternate metadata | ينجح فحص locale pair وhreflang للمقالات المنشورة |
| REQ-CALC-01 | Must | عند استخدام أي لغة أو تصميم جديد، تبقى نتيجة NexConvert كما هي | كل conversion وWebMCP eval الحالي ينجح دون تغيير tolerance أو عوامل التحويل |
| REQ-EMBED-01 | Must | عند فتح embed مع query string أو postMessage، تُملأ المدخلات وتظهر نتيجة حتمية | تمر اختبارات contract وsmoke للـembed في العربية والإنجليزية |
| REQ-DOC-01 | Should | عندما يستخدم مطور WebMCP، يجد أمثلة inputs/outputs/errors وversioning | توجد وثيقة API/WebMCP قابلة للقراءة واختبار أمثلتها آليًا |

## 5. المتطلبات غير الوظيفية والقيود

يجب أن يبقى الحساب محليًا وحتميًا، وأن تبقى عوامل التحويل في `data/units.json` وحده. يجب ألا تضيف الترجمة أو المحتوى اتصالًا خارجيًا مطلوبًا لتشغيل المحوّل. يجب الحفاظ على PWA وoffline assets وCSP وerror boundary وaccessibility الحالية. يجب أن تعمل الصفحات الأساسية دون overflow بعرض 360px، وأن تبقى أهداف التحكم مناسبة للمس. يجب ألا تنشئ البنية الجديدة خدمة backend أو قاعدة بيانات؛ ستظل مولدات Python وملفات JSON/Markdown هي الحد الأدنى الكافي.

## 6. البيانات والعقود

سيبقى `data/units.json` مصدر الحقيقة للوحدات وعوامل التحويل. ستضاف طبقة محتوى منفصلة مثل `data/content/*.json` أو ملفات Markdown front matter للمقالات والـFAQ والبيانات الوصفية، مع فصل اللغة والslug والعلاقات إلى الفئات والأزواج. ستضاف translation catalogs مثل `data/i18n/ar.json` و`data/i18n/en.json` للمفاتيح المشتركة في chrome والواجهة.

عقد `NexMCP.convert`, `listUnits`, `provenanceFor` و`explain_conversion` لا يتغير في النسخة الأولى من التوسع. يمكن أن تتغير النصوص المعروضة والـlabels فقط، بينما تبقى رموز الفئات والوحدات والمدخلات الرقمية مستقرة. يجب أن تعيد العقود أخطاء typed نفسها في اللغتين، مع رسائل عرض مترجمة خارج payload الحساب الأساسي عند الحاجة.

## 7. الحالات والاستثناءات

يجب اختبار اللغة الافتراضية عند عدم وجود لغة في الرابط، واللغة غير المدعومة، ومفتاح ترجمة مفقود، ومقال لا يملك نسخة مقابلة، وقيمة تحويل فارغة أو غير رقمية، وفئة أو وحدة غير موجودة، وquery string غير صالح في embed، وانقطاع الشبكة بعد تحميل الصفحة، وعرض الهاتف الضيق، وحفظ لغة أو theme قديم في localStorage. fallback المحافظ هو العربية للغة غير المدعومة، والنص الأصلي أو placeholder واضح لمحتوى اختياري مع تسجيل فشل build بدل إخفائه.

## 8. مصفوفة التتبع

| الشريحة | المتطلبات | الدليل المطلوب |
|---|---|---|
| UI redesign | REQ-UI-01..04 | snapshots/DOM smoke، فحص 360px، keyboard/accessibility، regression للمحوّل |
| i18n foundation | REQ-I18N-01..04 | schema/catalog tests، generated-page checks، lang/dir/hreflang checks |
| content system | REQ-CONT-01..03 | content schema، link checker، article generation، locale-pair checks |
| deterministic preservation | REQ-CALC-01 | `test_conversions.py`, `test_webmcp.js`, `test_webmcp_eval.js` |
| embed and docs | REQ-EMBED-01, REQ-DOC-01 | embed integration، API examples، typed-error contract |

## 9. الافتراضات والأسئلة المفتوحة

الافتراض التنفيذي هو أن الإنجليزية هي اللغة الثانية ذات الأولوية، وأن العربية تظل اللغة الافتراضية، وأن الملفات المولدة الحالية يمكن إعادة بنائها دون تغيير الروابط العربية. لا توجد أسئلة مانعة للبدء في إعادة التصميم وبنية i18n. قرار اللغات الثالثة، عدد المقالات الإنجليزية، وهوية النشر العام مؤجل حتى تُثبت الشريحة الأولى محليًا.

## 10. المخاطر والقرارات المعمارية المطلوبة

الخطر الرئيسي هو أن يؤدي إعادة كتابة القوالب إلى كسر روابط عربية أو عقود WebMCP أو أصول PWA. لذلك يجب إبقاء طبقة الحساب وcanonical data خارج التغيير، وإضافة اختبارات regression قبل تبديل القوالب. الخطر الثاني هو تضخم الترجمة والمحتوى داخل HTML؛ يعالج ذلك dataset منظم وschema يفشل مبكرًا عند المفاتيح أو الحقول الناقصة. الخطر الثالث هو اختلاف RTL/LTR في CSS؛ يعالج باستخدام logical properties وtests على اتجاهي العرض بدل نسخ CSS منفصل.

القرار الحالي هو **monolith static متعدد الطبقات**: canonical units، translation catalogs، content datasets، generators، assets، ثم صفحات HTML مولدة. لا توجد حاجة إلى microservices أو قاعدة بيانات في هذه الشريحة؛ يمكن تأجيل API عامة إلى مرحلة منفصلة بعد إثبات عقود WebMCP المحلية.
