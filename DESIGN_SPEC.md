# مواصفة تصميم نكسلونا (Nexluna Design Spec)

> مواصفة إلزامية للتصميم — تُطبق على `assets/css/style.css` والقوالب. أي تعديل مستقبلي يجب أن يلتزم بها، وتفحص البوابات (gates) وفقها.

## 1. الوحدات والأحجام

| البند | المواصفة | مبرر |
|---|---|---|
| أحجام CSS | `rem` للعناصر النصية والواجهات؛ `px` مسموح فقط داخل media queries والظلال والحدود الدقيقة (1px) | 99 حالة px ثابتة تخالف قابلية الوصول (تتجاهل إعداد المستخدم لخط المتصفح) |
| منطقة اللمس | حد أدنى 44×44px لكل عنصر تفاعلي (أزرار، أيقونات، tabs، chips تفاعلية) | WCAG 2.5.5 — Target Size (enhanced) 44×44، وiOS/Android design guidelines |
| أيقونات SVG داخل أزرار | 18-22px فقط داخل حاوية 44px (padding + display:flex:center) | الأيقونة ليست المنطقة اللمسية؛ الحاوية هي |
| زر Primary | min-height 48px، خط 15px وزن 600، ترويسة أفقية 24px | أكبر CTA في الصفحة |
| زر Secondary (btn-outline/btn-sm) | min-height 40px (44px للأجهزة اللمسية عبر media query) | تمييز هرمي واضح |
| حقل الإدخال | min-height 48px | اتساق مع الأزرار |
| التباعد الأساسي | 4px grid (padding/margin بمضاعفات 4px) | نظام إيقاعي متناسق |

## 2. حالات الأزرار (states)

كل عنصر تفاعلي يجب أن يملك أربع حالات معرفة صراحة:

| الحالة | المواصفة |
|---|---|
| `:hover` | لون/خلفية أغمق بنسبة 8-12% (أو overlay أسود 8%) + transition 180ms |
| `:focus-visible` | حلقة ring بسمك 2-3px بلون `--ring` مع `outline-offset: 2px` + `outline: none` على العناصر الافتراضية |
| `:active` | scale(0.98) أو إزاحة ظل لأعلى |
| `:disabled` | opacity 0.5 + cursor not-allowed + pointer-events none |

قاعدة: **hover يعالج المظهر، focus-visible يعالج الوصول**. استخدام `:focus-visible` (وليس `:focus`) يمنع ظهور الحلقات عند التنقل بالماوس ويحافظ عليها للوحة المفاتيح — المعيار الأحدث.

منحنى الحركة: `transition: color 180ms ease, background-color 180ms ease, box-shadow 180ms ease, transform 180ms ease`.

## 3. التباين اللوني (WCAG 2.1 AA)

| المستوى | الحد الأدنى |
|---|---|
| نص عادي (under 18px / bold under 24px) | 4.5:1 |
| نص كبير (≥18px regular / ≥24px bold) | 3:1 |
| عناصر واجهة وحدود تمييز (non-text contrast) | 3:1 |

**الحل المعتمد لمشكلة --accent/التدرج**: لا نغيّر هوية العلامة، بل نغيّر نص الزر:
- نص أزرار التدرج (swap-btn, grad) يظل أبيض لكن يُظلم التدرج بحيث يكون أغمق لون ≥ #0d9488 في كل مساره (حذف #2dd4bf من نهاية التدرج → يرفع التباين من 1.86 إلى ≥4.5).
- أزرار `--accent` الخالصة: نستخدم `--accent-600` (#0d9488) خلفية بنص أبيض (2.49 → 4.5:1)، ونحتفظ بـ #14b8a6 للعناصر غير النصية (أيقونات، حدود، خلفيات زخرفية).

تحقق بالفحص البرمجي `scripts/_audit_contrast.py` — لا يقبل إلا نسب ≥4.5 للنص العادي.

## 4. هرمية الأزرار

| النوع | المظهر | الاستخدام |
|---|---|---|
| `.btn-primary` | تدرج العلامة المظلمة + نص أبيض + ring على focus | CTA واحد رئيسي لكل سياق |
| `.btn-outline` | حدود primary-700 + خلفية بيضاء + نص primary-700 | إجراء ثانوي |
| `.btn-ghost` | خلفية شفافة + نص primary-700 + hover surface | إجراءات خفيفة |
| `.btn-sm` | 40-44px، خط 13px | أدوات داخل البطاقات |
| `.icon-btn` | 44×44px، أيقونة 20px | أزرار بدون نص |

## 5. خطوط

`clamp(1rem, ...)` للعناوين الرئيسية لتجاوب سلس بدل media queries متعددة، مع الحفاظ على RTL وline-height ≥ 1.5 للنص العربي.

## فحص الامتثال الدوري

1. `python3 scripts/_audit_contrast.py` — لا FAILs مسموحة.
2. `python3 scripts/_audit_buttons.py` — لا أزرار <44px (سوى أيقونات داخل حاويات)، لا عنصر بدون focus state.
3. `find . -name '*.css' | xargs ...` — عدّاد px خارج media queries ≤ حد معقول (يُخفض تدريجيًا).
