# Nexluna — خطة التطوير والتوسيع المستقبلية
_مُعدّة بصفة خبير تطوير — مرتّبة حسب الأثر مقابل الجهد (Impact / Effort)_

الوضع الحالي: 14 محوّلًا، مدوّنة (4 مقالات)، بحث ذكي حتمي، PWA، تصميم فاخر، أداء FCP ~260ms، مساهم واحد على `main`.

---

## المبادئ الحاكمة
1. **البقاء ثابتًا (static-first):** لا خادم، لا تكلفة تشغيل — كل شيء يُبنى بـ Python إلى HTML/CSS/JS.
2. **صحّة دائمة، بلا هلوسة:** أي ميزة "ذكية" حتمية تمرّ عبر `window.NexConvert`.
3. **الأداء غير قابل للتفاوض:** لا نضيف ميزة تكسر المسار الحرج.
4. **SEO عربي أولًا:** كل توسّع = صفحة قابلة للفهرسة بكلمات مفتاحية حقيقية.
5. **RTL + إتاحة (a11y) في كل مكوّن جديد.**

---

## المرحلة 1 — مكاسب سريعة عالية الأثر (Now)
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| 1.1 | **صفحات أزواج التحويل الشائعة** (مثل `km-to-mile`, `kg-to-lb`, `celsius-to-fahrenheit`) مُولّدة برمجيًا مع محتوى فريد + جدول مرجعي + FAQ JSON-LD | كلمات مفتاحية طويلة الذيل = أكبر مصدر زيارات بحثية | متوسط |
| 1.2 | **جداول تحويل مرجعية** أسفل كل محوّل (قيم شائعة جاهزة) | محتوى قابل للفهرسة + قيمة فورية بلا تفاعل | منخفض |
| 1.3 | **نسخ/مشاركة النتيجة** (زر نسخ + رابط عميق `?from=&to=&v=`) | التصاق واستخدام متكرّر | منخفض |
| 1.4 | **روابط عميقة للحالة** في المحوّل والبحث الذكي | مشاركة نتيجة محدّدة | منخفض |

## المرحلة 2 — توسيع المحتوى والفئات (Next)
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| 2.1 | فئات جديدة: **العملات (سعر ثابت/تعليمي)، الأحذية/الملابس، الطبخ بالأكواب، السوائل الطبية** | تغطية استعلامات يومية | متوسط |
| 2.2 | توسيع المدوّنة إلى 12+ مقالًا (دليل لكل فئة) مع ربط داخلي | سلطة موضوعية (topical authority) | متوسط |
| 2.3 | **حاسبات مركّبة** (BMI، مساحة غرفة، استهلاك وقود رحلة) | نية مستخدم أعلى | متوسط |

## المرحلة 3 — منتج وتفاعل (Later)
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| 3.1 | **المفضلة/الأخيرة** (localStorage) على الصفحة الرئيسية | عودة المستخدم | منخفض |
| 3.2 | **اختصارات لوحة المفاتيح** + وضع الأمر (⌘K) للبحث الذكي | حِرفية "craft bar" | متوسط |
| 3.3 | توسيع معجم البحث الذكي ليشمل كل الفئات الـ14 + جمل أكثر | تغطية أوسع | متوسط |

## المرحلة 4 — نمو وقياس (Ongoing)
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| 4.1 | **تحليلات خفيفة تحترم الخصوصية** (بلا كوكيز) | قرارات مبنية على بيانات | منخفض |
| 4.2 | **Lighthouse CI** في البناء + ميزانية أداء | منع الانحدار | متوسط |
| 4.3 | **hreflang / نسخة إنجليزية** للفئات الأعلى زيارة | سوق أوسع | مرتفع |

---

## التنفيذ في هذه الجولة (المرحلة 1) — ✅ مُنجزة
- [x] 1.1 مولّد صفحات أزواج التحويل (`build_pairs.py`) — 25 صفحة + فهرس `/convert/`، مُدرجة في sitemap (49 رابطًا) وفي التذييل
- [x] 1.2 جداول تحويل مرجعية (قيم شائعة) داخل كل صفحة زوج
- [x] 1.3 نسخ النتيجة + زر مشاركة (Web Share API مع بديل نسخ الرابط)
- [x] 1.4 روابط عميقة للحالة: `?from=&to=&v=` تُعبّئ المحوّل تلقائيًا، وشريط العنوان يتحدّث عند كل تحويل

**التحقق:** حساب مطابق تمامًا (1 كجم=2.204623 رطل، 5 كم=3.106856 ميل)، صفر أخطاء console غير الإعلانات، لقطات مؤكّدة.

## التنفيذ في هذه الجولة (المرحلة 2) — قيد التنفيذ
- [x] 2.2 توسيع المدوّنة: أُضيفت 3 مقالات جديدة (تحويل الوزن، تحويل السرعة، البحث بلغة طبيعية) → 7 مقالات، مع صور hero أصلية بأسلوب العلامة وربط داخلي إلى `/converters/` و`/convert/`، sitemap = 52 رابطًا
- [ ] 2.1 فئات جديدة (العملات التعليمية، الملابس/الأحذية، الطبخ بالأكواب)
- [ ] 2.3 حاسبات مركّبة (BMI، مساحة غرفة، استهلاك وقود)

**التالي:** استكمال 2.1 و2.3.

## تدقيق شامل وإصلاح (SEO + إتاحة + أداء + جودة الكود) — ✅ مُنجز
تقمّصتُ أدوار خبراء متعددين (SEO، الإتاحة، الأداء، جودة الكود) وأجريتُ تدقيقًا آليًا على 55 صفحة، ثم أصلحتُ كل ما اكتشفته:
- [x] **إتاحة — ترتيب العناوين:** إصلاح قفزات مستوى العناوين في التذييل (`h4`→`h2`) وبطاقات صفحات الفهارس (`h3`→`h2` في `/convert/` والمدوّنة) مع إضافة محدّدات CSS للحفاظ على المظهر
- [x] **SEO — عناوين طويلة:** تقصير عنوان صفحة البيانات، وإضافة آلية `meta_title` مستقلة لمقالَي الحرارة والبيانات
- [x] **SEO — أوصاف ميتا قصيرة:** توسيع أوصاف صفحات 404/offline/contact-success إلى وصف كامل
- [x] **SEO — وسوم OG/Twitter:** إضافتها لصفحة `/convert/` الرئيسية
- [x] **بصري (خلل حقيقي):** صفحة `/convert/` كانت تستخدم فئة `cards-grid` غير المعرّفة في CSS بدل `grid grid-cards` — أُصلحت لتظهر شبكة البطاقات المتجاوبة بشكل صحيح
- [x] **الحفاظ على ملف تحقّق Google** (`googlee1aef9f98208b972.html`) دون أي تعديل

**التحقق:** التدقيق النهائي = صفر مشاكل عبر 55 صفحة، حساب المحوّل مطابق (5 كم = 3.106856 ميل)، لقطات مؤكّدة لكل الصفحات الرئيسية.

---

# خطة التطوير المستقبلية — بعيون خبراء متعددين
_تحليل متعدد الأدوار (Product · Frontend Architecture · SEO · Performance · Accessibility · DevOps · Security) — مرتّب حسب الأثر مقابل الجهد._

> **فلسفة تقنية:** نتبنّى **أحدث المعايير والتقنيات المناسبة للسياق** لا الأحدث لمجرّد الحداثة. الموقع static-first فائق الأداء؛ أي تقنية جديدة يجب أن تحافظ على هذا التفوّق (FCP ~260ms) لا أن تكسره.

## 1) خبير المنتج (Product)
| # | البند | القيمة | الجهد |
|---|------|--------|-------|
| P1 | لوحة "المفضّلة + آخر التحويلات" على الصفحة الرئيسية (localStorage) | عودة المستخدم | منخفض |
| P2 | مشاركة نتيجة كصورة (Canvas → PNG) لوسائل التواصل | نمو عضوي | متوسط |
| P3 | حاسبات مركّبة (BMI، مساحة غرفة، تكلفة وقود الرحلة) | صفحات جديدة عالية النية | متوسط |
| P4 | وضع "لوحة أوامر" (⌘K) للبحث الذكي عن أي تحويل فورًا | حِرفية الاستخدام | متوسط |

## 2) مهندس الواجهة (Frontend Architecture)
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| F1 | ترحيل تدريجي إلى **TypeScript** لمنطق المحوّل (`converter.ts`) مع بناء إلى JS عبر `esbuild` | أمان أنواع + صيانة أطول أمدًا | متوسط |
| F2 | مكوّنات ويب أصيلة (**Web Components / Custom Elements**) للمحوّل بدل الاعتماد على DOM يدوي | إعادة استخدام + عزل | متوسط |
| F3 | تبنّي **CSS الحديث** المتوافق: `@layer`, `:has()`, `container queries`, `text-wrap: balance` مع fallback | تصميم أقوى بأقل كود | منخفض |
| F4 | نظام تصميم موثّق (Design Tokens في ملف JSON مصدرًا واحدًا يولّد CS+ docs) | اتساق عبر المشاريع | متوسط |

## 3) خبير SEO
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| S1 | توسيع صفحات أزواج التحويل من 25 إلى 100+ زوج تلقائيًا من جدول المعاملات | ذيل طويل ضخم | متوسط |
| S2 | `hreflang` + نسخة إنجليزية للفئات الأعلى زيارة (يفعّل اسم المطوّر الإنجليزي في التذييل تلقائيًا) | سوق عالمي | مرتفع |
| S3 | Schema إضافية: `HowTo` لكل دليل، `Breadcrumb` على كل صفحة زوج | نتائج غنية | منخفض |

## 4) خبير الأداء (Performance)
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| PF1 | **Lighthouse CI** في خط البناء + ميزانية أداء (تفشل عند تجاوزها) | منع الانحدار | متوسط |
| PF2 | `Speculation Rules API` (prerender/prefetch) للروابط الداخلية الشائعة | تنقّل شبه فوري | منخفض |
| PF3 | مراجعة تقسيم الخطوط (subset إضافي للأرقام العربية) | تقليل حجم الخط | منخفض |

## 5) خبير الإتاحة (Accessibility)
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| A1 | فحص a11y آلي في CI (axe-core) مع بوابة فشل | جودة دائمة | متوسط |
| A2 | دعم `prefers-reduced-motion` و`prefers-contrast` بالكامل عبر كل الحركات | شمول أوسع | منخفض |
| A3 | اختبار قارئ شاشة عربي (NVDA/VoiceOver) موثّق | تجربة حقيقية | متوسط |

## 6) DevOps + الأمان (DevOps & Security)
| # | البند | لماذا | الجهد |
|---|------|-------|-------|
| D1 | **GitHub Actions**: بناء تلقائي (تشغيل كل `build_*.py`) + تدقيق + Lighthouse عند كل push | لا بناء يدوي | متوسط |
| D2 | مراجعة `Content-Security-Policy` مشدّدة في `netlify.toml` (nonce للسكربتات) | تصلّب أمني | متوسط |
| D3 | ملفات مجتمع للمستودع: `LICENSE` (MIT)، `CONTRIBUTING.md`، `SECURITY.md` | احترافية المستودع | منخفض |

---

**قاعدة الإسناد:** كل توسّع مستقبلي يحافظ على سطر كريدت المطوّر (`DEV_CREDIT`) و JSON-LD (`DEV_PERSON_JSONLD`) كمصدر واحد في `build_pages.py` — تغيير أو إزالة الكريدت = سطر واحد فقط.
